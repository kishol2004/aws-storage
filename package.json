{
  "name": "securedoc-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Secure Cloud Document Management System",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:all": "npx concurrently \"npm --prefix backend run build\" \"npm --prefix frontend run dev\"",
    "start": "npm run deploy:backend && npm run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build && node -e \"fs.cpSync('frontend/dist', 'dist', {recursive: true})\"",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "deploy:backend": "npm --prefix backend run deploy",
    "synth:backend": "npm --prefix backend run synth"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "vite": "^6.2.0",
    "vercel": "^59.5.0"
  }
}
